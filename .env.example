# Output of `npm run musickit:token` (see README.md for the full setup flow).
VITE_MUSICKIT_DEVELOPER_TOKEN=

# Optional - shown to Apple during authorization; defaults are fine for local dev.
VITE_MUSICKIT_APP_NAME=Mixing Table
VITE_MUSICKIT_APP_BUILD=0.1.0
